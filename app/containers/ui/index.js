import React, {Component, PropTypes} from "react";
import {connect} from "react-redux";

export default class UITagContainer extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  };
  
  componentWillMount() {
  
  };
  
  render() {
    return (
      <div>test</div>
    );
  };
}
